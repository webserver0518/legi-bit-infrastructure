apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "legibit.fullname" . }}-test-connection"
  labels:
    {{- include "legibit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "legibit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
